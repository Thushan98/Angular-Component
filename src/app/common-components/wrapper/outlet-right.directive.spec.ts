import { OutletRightDirective } from './outlet-right.directive';

describe('OutletRightDirective', () => {
  it('should create an instance', () => {
    const directive = new OutletRightDirective();
    expect(directive).toBeTruthy();
  });
});

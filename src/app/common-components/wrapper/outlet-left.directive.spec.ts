import { OutletLeftDirective } from './outlet-left.directive';

describe('OutletLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new OutletLeftDirective();
    expect(directive).toBeTruthy();
  });
});
